import { SpyAnchorDirective } from './spy-anchor.directive';

describe('SpyAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new SpyAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
