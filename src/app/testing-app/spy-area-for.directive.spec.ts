import { SpyAreaForDirective } from './spy-area-for.directive';

describe('SpyAreaForDirective', () => {
  it('should create an instance', () => {
    const directive = new SpyAreaForDirective();
    expect(directive).toBeTruthy();
  });
});
